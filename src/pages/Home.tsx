import Table from '@/components/MembersTable'
import React from 'react'

type Props = {}

const Home = (props: Props) => {
  return (
    <div>
      <Table />
    </div>
  
  )
}

export default Home